

apiVersion:v1
kind:pod
matadata:
  name: redis-pod
  labels:
    name: redis-pod
	app: demo-voting-app
	
	
spec:
  containers: 
    name: redis-pod
	image: redis
	ports:
	  containerPort: 6379